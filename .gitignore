# ── Rust ──────────────────────────────────────────────────────────────────────
/target/
**/target/
**/*.rs.bk
*.pdb

# Generated by build.rs — never commit, always regenerate with cargo build
src/generated/layout.rs
python/nexus/_layout.py

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
**/__pycache__/
*.py[cod]
*$py.class
*.pyo

# Virtual environments
.venv/
venv/
env/
.env/
ENV/

# Distribution / packaging
dist/
build/
*.egg-info/
*.egg
.eggs/
MANIFEST

# PyO3 / maturin build artifacts
*.so
*.pyd
*.dylib
*.dll
nexus_native*.so
nexus_native*.pyd

# mypy cache
.mypy_cache/
.dmypy.json
dmypy.json

# pytest
.pytest_cache/
.coverage
htmlcov/

# ── NEXUS Runtime Data ────────────────────────────────────────────────────────
# SQLite database — runtime state, not source
nexus.db
nexus.db-shm
nexus.db-wal
*.db
*.db-shm
*.db-wal

# Episodic memory journals — generated at runtime
memory/journals/

# Embedding model weights — too large for git, download separately
models/
*.onnx
*.pt
*.bin
*.safetensors
*.gguf

# Generated priors (authored PRIORS.md is tracked, generated output is not)
priors/generated/

# ── Editor and OS ─────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# ── Environment and Secrets ───────────────────────────────────────────────────
.env
.env.local
.env.production
*.key
*.pem
secrets/

# ── Benchmarks and Profiling ──────────────────────────────────────────────────
criterion/
flamegraph.svg
perf.data
perf.data.old

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log
logs/